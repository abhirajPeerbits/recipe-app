import { Component,EventEmitter, Output } from '@angular/core';
 

@Component({
    selector : 'app-header',
    templateUrl:  './header.component.html',
    styleUrls : ['./header.component.css'],
}) 
export class HeaderComponent {
// create custome event. pass < string> bcoz we pass feturn so its a string data.
// @output bcoz we need to use parant component (appComponent)
 


}